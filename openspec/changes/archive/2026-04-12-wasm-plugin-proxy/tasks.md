## 1. プロジェクトセットアップ

- [x] 1.1 `go mod init` でモジュールを初期化し、Go バージョンを固定する
- [x] 1.2 ディレクトリ構成（`cmd/xynon`, `internal/proxy`, `internal/plugin`, `internal/plugin/abi`, `internal/config`, `examples/plugins`）を作成する
- [x] 1.3 `github.com/tetratelabs/wazero` と `github.com/fsnotify/fsnotify` を依存に追加する
- [x] 1.4 `CGO_ENABLED=0 go build ./...` が通ることをローカルで確認する

## 2. 設定ローダ（proxy-config）

- [x] 2.1 YAML 設定スキーマ（リスンアドレス、プラグインディレクトリ、プラグインチェーン順序、実行時間・メモリ上限）を Go 構造体として定義する
- [x] 2.2 ファイルパスから設定を読み込み、必須項目欠落時は欠落フィールド名を含むエラーを返すローダを実装する
- [x] 2.3 設定ローダの単体テスト（正常系、ファイル無し、必須欠落）を追加する
- [x] 2.4 `examples/config.yaml` を追加してリスンアドレスとチェーン例を示す

## 3. プラグイン ABI 定義

- [x] 3.1 ABI バージョン番号と、ホストが提供するインポート関数シグネチャ（`get_header`, `set_header`, `set_status`, `short_circuit`, `log`）を `internal/plugin/abi` にドキュメント化する
- [x] 3.2 プラグインがエクスポートすべき関数（`xynon_abi_version`, `on_request`, `on_response`）を定義する
- [x] 3.3 ハンドル ID のライフタイム規約（リクエスト単位で有効、フック終了時に無効化）をドキュメント化する

## 4. WASM プラグインランタイム（wasm-plugin-runtime）

- [x] 4.1 wazero の `Runtime` と `CompilationCache` を初期化するランタイム起動コードを実装する
- [x] 4.2 `.wasm` ファイルをコンパイル・インスタンス化し、ABI バージョンのエクスポートを検証するローダを実装する
- [x] 4.3 ホスト関数（`get_header`, `set_header`, `set_status`, `short_circuit`, `log`）を `env` 名前空間に登録する
- [x] 4.4 リクエスト/レスポンスごとにハンドル ID を発行し、フック終了時に無効化するハンドルテーブルを実装する
- [x] 4.5 フック呼び出しに `context.WithTimeout` を適用し、メモリ上限を wazero の設定で強制する
- [x] 4.6 非対応 ABI バージョン、エクスポート関数欠落、実行エラー、タイムアウト、メモリ超過をそれぞれ専用のエラー型として返す
- [x] 4.7 ランタイムの単体テスト（正常ロード、ABI 不一致、タイムアウト、無効ハンドル操作）を追加する

## 5. プロキシホスト（proxy-host）

- [x] 5.1 `net/http` ベースのリスナと、`httputil.ReverseProxy` または手書きトランスポートで upstream 転送を行うハンドラを実装する
- [x] 5.2 `CONNECT` メソッドに対して TCP トンネリングを行う経路を追加する
- [x] 5.3 リクエスト処理開始時に現在のチェーンスナップショットを 1 度だけ取得し、ローカル変数で使い続けるよう実装する
- [x] 5.4 チェーン順に `on_request` を実行し、短絡応答が指示されたら以降のプラグインと upstream 送信をスキップする
- [x] 5.5 upstream からのレスポンス取得後にチェーンの `on_response` を実行し、ヘッダ変更をクライアント応答に反映する
- [x] 5.6 プラグイン実行エラー発生時は当該プラグインのみスキップしてリクエスト処理を継続し、エラーをログ出力する
- [x] 5.7 プロキシホストの統合テスト（素通し、ヘッダ書き換え、短絡応答、プラグインエラー時の継続動作）を追加する

## 6. チェーンスナップショットの共有

- [x] 6.1 `atomic.Pointer[Chain]` を保持する `ChainRegistry` 型を実装する
- [x] 6.2 リクエストハンドラから `Load()` で現在のチェーンを取得する経路を配線する
- [x] 6.3 `Store()` による差し替えと、旧世代が進行中リクエストの参照で保持される挙動を明示するテストを追加する

## 7. ホットリロード（plugin-hot-reload）

- [x] 7.1 fsnotify でプラグインディレクトリを再帰的に監視するウォッチャを実装する
- [x] 7.2 検知イベントを短いデバウンス期間（例 200ms）でまとめ、書き込み安定性を `os.Stat` サイズ・ modtime の 2 回一致で確認する
- [x] 7.3 デバウンス発火時に「設定に宣言されたプラグイン順序で」新しいチェーンを構築する
- [x] 7.4 いずれかのプラグインのロードに失敗した場合は、差し替えを中止し現行チェーンを維持してエラーをログ出力する
- [x] 7.5 構築成功時は `ChainRegistry.Store()` で新チェーンにスワップする
- [x] 7.6 ホットリロードの統合テスト（追加、更新、削除、不正ファイル、部分書き込み）を追加する

## 8. CLI エントリポイント

- [x] 8.1 `cmd/xynon/main.go` に設定ファイルパスを受け取るフラグを実装する
- [x] 8.2 設定ローダ → ランタイム初期化 → 初回チェーン構築 → ホットリロード開始 → プロキシリスナ起動の順で配線する
- [x] 8.3 シグナル（SIGINT/SIGTERM）で listener、ウォッチャ、ランタイムを順に安全に停止する
- [x] 8.4 ホットリロード無効化フラグ（`--no-hot-reload`）を実装し、起動時のチェーンで固定する

## 9. サンプルプラグインとビルドパイプライン

- [x] 9.1 TinyGoでビルドできる最小サンプルプラグイン（例: `X-Xynon` ヘッダを付与する）を `examples/plugins` に追加する
- [x] 9.2 サンプルプラグインのビルドコマンドを README/Makefile に記載する
- [x] 9.3 プロキシを起動してサンプルプラグインのホットリロード動作を手動確認する手順を記載する

## 10. ドキュメント

- [x] 10.1 リポジトリルートに最小の実行手順（設定例、ビルド、起動、動作確認）を追記する
- [x] 10.2 ABI のバージョンポリシー（変更時の互換性ルール）をドキュメント化する
